---
description: Pull latest master and create a new feature branch
---
1. Switch to master
// turbo
git checkout master

2. Pull latest changes
// turbo
git pull origin master

3. Create branch name based on feature
// turbo
git checkout -b features/[branch-name]
