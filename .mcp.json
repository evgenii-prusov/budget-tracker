{
  "mcpServers": {
    "budget-tracker": {
      "type": "streamable-http",
      "url": "http://localhost:8000/mcp/",
      "headers": {
        "Authorization": "Bearer ${API_KEY}"
      }
    }
  }
}